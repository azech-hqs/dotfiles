# Generated by `chezmoi init` from this template. Not deployed into $HOME.
# Pins chezmoi's source directory to wherever this repo was cloned, so that
# `chezmoi apply` works without passing --source on every invocation.
sourceDir = {{ .chezmoi.sourceDir | quote }}
